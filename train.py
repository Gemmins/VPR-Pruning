# train should take all arguments necessary for training and return trained network
# this is where custom methods to train networks can be used, just as long as the model itself is returned
def train(backbone, aggregation, datasets_folder, dataset_name):

    return "model"

